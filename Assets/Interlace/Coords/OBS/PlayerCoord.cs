using UnityEngine;

namespace Interlace.OBS
{
    public class PlayerCoord : MonoBehaviour
    {
        internal PlayerHandler PlayerHandler { get; set; }
        internal ActorHandler ActorHandler { get; private set; }

        internal void Setup()
        {

        }
    }
}
